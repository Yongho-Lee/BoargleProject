
import { createSlice } from '@reduxjs/toolkit'



let arrival = createSlice({
    name: 'arrival',
    initialState: [],
    reducers : {

    }

})

//export let {  } = arrival.actions;
export default arrival
